import numpy as np
import matplotlib.pyplot as plt

A = np.array([[1, -np.sqrt(3.) / 3], [0, 2 * np.sqrt(3.) / 3]],dtype=float)

def calculaDistorsioMalla(X, T):
    
    raise Exception('CODE HERE THE DISTORTION OF THE MESH')
    
    someNumber=0.0
    
    return someNumber
